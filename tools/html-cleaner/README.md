# HTML Cleaner

A browser-only utility for removing selected attributes from HTML. Enter comma-separated attribute names such as `style,class`; the cleaned HTML updates immediately and can be copied to the clipboard.

All parsing and filtering happens locally in the browser.
